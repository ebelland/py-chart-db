"""Data Hub.

The name and the version live here because two dialogs and a window title
need them, and a string repeated in three places is a string that ends up
disagreeing with itself.  Bump ``APP_VERSION`` here and nowhere else.
"""

APP_NAME: str = "Data Hub"
APP_VERSION: str = "0.1.0"
